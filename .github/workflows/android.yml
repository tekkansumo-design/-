name: Android APK

on:
  push:
    paths:
      - 'android/**'
      # 画面の生成元と生成スクリプト。ここを変えても APK に影響する
      - 'bookoff_web.py'
      - 'tools/**'
      - '.github/workflows/android.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check generated asset is up to date
        run: python3 tools/gen_android_asset.py --check

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools platforms;android-35 build-tools;35.0.0'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('android/**/*.gradle.kts', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Test 最安値サーチの抽出
        working-directory: android
        run: ./gradlew --no-daemon :cdprice:testDebugUnitTest

      - name: Build debug APK
        working-directory: android
        run: ./gradlew --no-daemon assembleDebug

      - name: Rename APK
        run: |
          mkdir -p out
          cp android/app/build/outputs/apk/debug/app-debug.apk \
             "out/bookoff-checker-${GITHUB_RUN_NUMBER}.apk"
          cp android/cdprice/build/outputs/apk/debug/cdprice-debug.apk \
             "out/cd-price-${GITHUB_RUN_NUMBER}.apk"
          ls -la out

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: out/*.apk

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: apk-${{ github.run_number }}
          name: build ${{ github.run_number }}
          body: |
            スマホのブラウザでこのページを開き、下の .apk をタップするとインストールできます。
            「提供元不明のアプリ」の許可を求められたら許可してください。

            - `bookoff-checker-*.apk` … BOOKOFF 在庫店舗チェッカー
            - `cd-price-*.apk` … 中古CD最安値サーチ（ブックオフ／ネットオフ／駿河屋／メルカリ／HMV）
          files: out/*.apk
          generate_release_notes: false
